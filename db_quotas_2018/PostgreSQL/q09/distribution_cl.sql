CREATE TABLE q09.distribution_cl(
	id_distribution_cl int NOT NULL,
	distribution_cl varchar(150) NULL,
	creation_date date NULL,
    CONSTRAINT PK_distribution_cl PRIMARY KEY 
    (
        id_distribution_cl
    )
);

